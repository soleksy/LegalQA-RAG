{
    "mongo_db": {
        "host": "localhost",
        "port": 5555,
        "uri": "mongodb://localhost:5555/"
    },

    "db_config": {
        "db_name": "law",
        "db_path": "/mongodb/data/"
    }
}